import { WheelScrollDirective } from './wheel-scroll.directive';

describe('WheelScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new WheelScrollDirective();
    expect(directive).toBeTruthy();
  });
});
